mixin video(video)
    div
        h4 #{video.title}
        ul
            li #{video.rating}/5.
            li comments: #{video.comments}.
            li Posted #{video.createdAt}.
            li #{video.views} views.