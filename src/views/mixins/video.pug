mixin video(video)
    div
        a(href="/videos/"+video.id)
            h4 #{video.title}
        ul
            li #{video.rating}/5.
            li comments: #{video.comments}.
            li Posted #{video.createdAt}.
            li #{video.views} views.